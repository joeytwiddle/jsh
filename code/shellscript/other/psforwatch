# jsh-depends-ignore: pid
# pstree -ap
# ps aux | dropcols 3 4 5 6 8 9
ps --cols 160 -o ppid,pid,user,args -A | grep -v "\<$$\>" ## Thanks to ppid and pid, both this sh, the ps, and the grep are removed =)
# | ungrep $$ ungrep psforwatch defunct sleep bash
# "%p %u %a"
