for X in *.zip; do
	Y=`echo "$X" | sed 's/\.zip$//'`
	mkdir "$Y"
	cd "$Y"
	unzip "../$X"
	# And because it always segfaults first time round:
	unzip "../$X"
	cd ..
done

for X in *.tar.gz *.tgz; do
	Y=`echo "$X" | sed 's/\(\.tar.gz\|\.tgz\)$//'`
	mkdir "$Y"
	cd "$Y"
	tar xfz "../$X"
	cd ..
done
