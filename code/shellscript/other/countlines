#!/bin/sh

AWKPRINT=""
if test "$2" != ""; then
	AWKPRINT='FILENAME "	"'
fi

for X
do

	awk " END { print $AWKPRINT NR } " "$X"

	# LINES=0;
	# cat "$X" |
	# while read L; do
	# LINES=$(($LINES+1));
	# echo "$LINES"
	# done;
	# echo "$LINES"

done
