#!/bin/sh

FAVOURITE_BROWSERS='galeon skipstone konqueror mozilla netscape encompass nautilus amaya lynx links w3m w3c'
FAVOURITE_TERM_BROWSERS='lynx links w3c'

ARGS="$@"
if test "x$ARGS" = "x"; then
	ARGS="$JPATH/org/jumpgate.html"
fi

if xisrunning; then
	runoneof "$FAVOURITE_BROWSERS" "$@" &
else
	runoneof "$FAVOURITE_TERM_BROWSERS" "$@"
fi

# netscape "file://$PWD/$@" &
