xset fp- unix/:7101 2>/dev/null # really I only want to ungrep "warning, no entries deleted from font path" the error stream, in case anything unexpected is wrong with the command.
`jwhich wine` "$@"
